.vercel

# Secrets — real config holds the Resend API key
includes/config.php

# Cloudflare local dev secrets (wrangler pages dev)
.dev.vars
.dev.vars.*
!.dev.vars.example

# Cloudflare Pages build output
dist/

# Internal / private docs — not for the repo
Information Required from Tegrach.docx
*.docx
*.doc
