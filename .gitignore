# Vercel
.vercel

# Secrets — real PHP config holds the Resend API key
platform/php/config.php

# Cloudflare local dev secrets (wrangler pages dev)
.dev.vars
.dev.vars.*
!platform/cloudflare/.dev.vars.example

# Vercel local env
.env
.env.*
!.env.example

# Build output
dist/
node_modules/

# Cloudflare wrangler local cache/state
.wrangler/

# Design source + internal reference material (kept locally, not in the repo)
design/
*.docx
*.doc
